- if current_user == @user
  %h2 Your Sites
- else
  %h2 #{h @user.display_name}'s Sites
%ul.stories#top-stories= render :partial => 'sites/site', :collection => @sites
- if @sites.empty?
  %p.empty This user currently has no sites.
#pagination= will_paginate @sites

